# Heartbeat — Priya (SDR)

Each heartbeat:
1. Read `commons/ceo-directives.md` and your `inbox/`.
2. Check open issues labelled `sdr`.
3. Add to the prospect list or draft outreach — never send without review.
4. File notes in `work/`, move handled mail to `inbox/read/`.
