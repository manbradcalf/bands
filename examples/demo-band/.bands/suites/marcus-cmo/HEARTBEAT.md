# Heartbeat — Marcus (CMO)

Each heartbeat:
1. Read `commons/ceo-directives.md` and your `inbox/`.
2. Check open issues labelled `cmo`.
3. Advance the launch narrative — one concrete artifact per heartbeat.
4. File drafts in `work/`, move handled mail to `inbox/read/`.
