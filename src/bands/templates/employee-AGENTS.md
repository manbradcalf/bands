# {{ employee.name }}'s Agents

Subagents you can spawn for well-scoped tasks. Use the Agent tool.

## Guidelines

- Spawn for independent, well-scoped tasks
- Run multiple agents in parallel when possible
- Handle quick tasks yourself (don't over-delegate)
- Subagents don't have their own memory scope

## Roster

<!-- Define subagent roles here as needed -->
